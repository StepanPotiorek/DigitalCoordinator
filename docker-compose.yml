services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - sqlite-data:/app/data
      - uploads-data:/app/public/uploads
    env_file:
      - .env.production
    restart: unless-stopped

volumes:
  sqlite-data:
  uploads-data:
